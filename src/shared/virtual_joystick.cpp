#include "virtual_joystick.h"

using namespace Shared;

void VirtualJoystick::draw()
{
	/*GRAPHICS->begin();
	{
		auto transform = Graphics::Transform();

		transform.setSize({ 64.0f, 64.0f });
		transform.setAnchor({ 0.25f, 0.75f });
		transform.setPivot({ 0.5f, 0.5f });

		GRAPHICS->drawCircle(transform, 32, { 1.0f, 1.0f, 1.0f, 0.25f });
	}
	{
		auto transform = Graphics::Transform();

		transform.setSize({ 24.0f, 24.0f });
		transform.setAnchor({ 0.25f, 0.75f });
		transform.setPivot({ 0.5f, 0.5f });

		GRAPHICS->drawCircle(transform, 32, { 1.0f, 1.0f, 1.0f, 0.25f });
	}
	GRAPHICS->end(); */
}